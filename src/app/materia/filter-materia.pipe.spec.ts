import { FilterMateriaPipe } from './filter-materia.pipe';

describe('FilterMateriaPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMateriaPipe();
    expect(pipe).toBeTruthy();
  });
});
