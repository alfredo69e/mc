import { PipeProfesorPipe } from './pipe-profesor.pipe';

describe('PipeProfesorPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeProfesorPipe();
    expect(pipe).toBeTruthy();
  });
});
