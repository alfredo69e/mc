import { FilterAlumnoPipe } from './filter-alumno.pipe';

describe('FilterAlumnoPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAlumnoPipe();
    expect(pipe).toBeTruthy();
  });
});
